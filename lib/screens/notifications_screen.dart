import 'package:flutter/material.dart';

class NotifacationsScreen extends StatelessWidget {
  const NotifacationsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('ventana de notificaciones'),
    );
  }
}